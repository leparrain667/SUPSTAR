{
  "name": "supstar-backend",
  "version": "1.0.0",
  "description": "API REST SUPSTAR - plateforme de recommandation de lieux",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "test": "node --test --test-concurrency=1 tests/*.test.js",
    "test:unit": "node --test tests/csv.test.js tests/import-validation.test.js",
    "test:integration": "node --test --test-concurrency=1 tests/api.integration.test.js",
    "prisma:generate": "prisma generate",
    "prisma:studio": "prisma studio",
    "db:apply-schema": "node scripts/apply-schema.js"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "multer": "^2.2.0",
    "passport": "^0.7.0",
    "passport-google-oauth20": "^2.0.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7",
    "prisma": "^5.20.0"
  }
}
